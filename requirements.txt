# Core Dependencies
python-dotenv>=1.0.0

# PDF Processing (only PyMuPDF/fitz is used)
PyMuPDF>=1.24.0

# Vector Store (PostgreSQL + pgvector)
psycopg2-binary>=2.9.9
pgvector>=0.2.4
numpy>=1.26.0

# HTTP / LLM Integration (NVIDIA & Groq APIs via requests)
requests>=2.31.0

# API Server
fastapi>=0.115.0
uvicorn>=0.30.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
pydantic>=2.0.0
